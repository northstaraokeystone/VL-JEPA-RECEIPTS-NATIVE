# Core dependencies
numpy>=1.24.0

# Optional: BLAKE3 for faster hashing (falls back to SHA256 if not available)
blake3>=0.3.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
black>=23.0.0
isort>=5.0.0
mypy>=1.0.0
